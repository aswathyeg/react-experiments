import React from "react";

const Slides = ({ slides }) => {
  return (
    <div>
      <button>previous</button>
      <button>current</button>
      <button>Next</button>
    </div>
  );
};

export default Slides;
