import React from "react";
import { useState, useEffect } from "react";
const FetchData = () => {
  useEffect(() => {}, []);
  return <div>{`Below is the fetched data ${data}`}</div>;
};

export default FetchData;
